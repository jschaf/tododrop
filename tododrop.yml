database:
  driverClass: org.postgresql.Driver
  # We include the user and password in the JDBC_DATABASE_URL
  url: ${JDBC_DATABASE_URL} # jdbc:postgresql://localhost:5432/todo

schema: tododrop

flyway:
  locations:
    - db/migration
  schemas:
    - tododrop
